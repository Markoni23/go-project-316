# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.wasm

# Test binary, generated with "go test -c"
[a-zA-Z]*.test
[a-zA-Z]*.test.exe

# Output of the go coverage tool, specifically when used with the -o flag
*.out

# Dependency directories ( go.mod keeps track of them, so they are not needed in VCS)
# go 1.11+
vendor/

# Environment files
.env

tmp/
bin/

.idea/